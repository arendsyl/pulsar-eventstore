my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
  pulsar {
    url = ${ADDON_PULSAR_BINARY_URL}
    tenant = ${ADDON_PULSAR_TENANT}
    ns = ${ADDON_PULSAR_NAMESPACE}
    token = ${ADDON_PULSAR_TOKEN}
  }
}
