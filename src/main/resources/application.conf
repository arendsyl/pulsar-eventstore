my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
  pulsar {
    url = ${PULSAR_URL}
    tenant = ${PULSAR_TENANT}
    ns = ${PULSAR_NS}
    token = ${PULSAR_TOKEN}
  }
}
